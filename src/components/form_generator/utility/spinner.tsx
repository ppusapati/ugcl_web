import { component$ } from "@builder.io/qwik";

/**
 * Spinner provide a visual cue that an action is being processed.
 */
export const P9ESpinner = component$(() => (
  <div class="spinner" aria-label="Loading" />
));
